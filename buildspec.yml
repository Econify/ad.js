version: 0.2

phases:
  install:
    commands:
      - npm install -g npm@latest
      - npm install
  pre_build:
      - npm run lint:no-fix
      - npm run test
  # build:
  #   commands:
  #     - npm run release

